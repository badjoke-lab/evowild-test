{
  "name": "evowild-test",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test:e2e": "playwright test",
    "predev": "node scripts/generate-s-model.mjs && npm run build:lane4-vendor",
    "prebuild": "node scripts/generate-s-model.mjs && npm run build:lane4-vendor",
    "build:lane4-vendor": "mkdir -p public/lane4 && browserify vendor/old-school-racing/evowild-entry.js -o public/lane4/old-school-racing.js"
  },
  "dependencies": {
    "three": "^0.181.0",
    "underscore": "^1.13.7"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "vite": "^7.1.7",
    "browserify": "^17.0.1"
  }
}
