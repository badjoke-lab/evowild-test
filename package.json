{
  "name": "evowild-test",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test:e2e": "playwright test",
    "predev": "node scripts/generate-s-model.mjs",
    "prebuild": "node scripts/generate-s-model.mjs && npm run build:lane4-v4",
    "build:lane4-v4": "npm ci --prefix vendor/kart-royale && npm run build --prefix vendor/kart-royale -- --base=/evowild-test/lane4-v4/ && rm -rf public/lane4-v4 && mkdir -p public/lane4-v4 && cp -R vendor/kart-royale/dist/. public/lane4-v4/"
  },
  "dependencies": {
    "three": "^0.181.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "vite": "^7.1.7"
  }
}
